using QR.Enums;

namespace QR.Utilities
{
    public class BitUtility
    {
        // public static void BitOrientation((int x, int y) initPosition, int dataSize, BytePattern pattern, out (int,int) nextInitPosition, out BytePattern nextPattern)
        // {
        //     while (dataSize > 0)
        //     {
        //         switch (pattern)
        //         {
        //             case BytePattern.Up:
        //                 break;
        //         }
        //     }
        //     nextInitPosition = (initPosition.x - dataSize / 2, initPosition.y - dataSize / 2);
        // }
    }
}