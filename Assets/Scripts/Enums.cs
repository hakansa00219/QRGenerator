using UnityEngine;

namespace QR.Enums
{
    public enum BytePattern : byte
    {
        Up = 0,
        Down = 1,
        Clockwise = 2,
        CtrClockwise = 3,
        Encoding = 4,
        End = 5,
    }
}

